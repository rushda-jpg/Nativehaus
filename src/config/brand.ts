// Native red — used sparingly, as an identifying accent only (plot outline
// pulse, calibration/location markers, tiny UI details). Never the
// interface's base color. Keep in sync with `--native-red` in index.css.
export const NATIVE_RED = "#c1272d";
